pipeline {
    agent any 
    stages {
        stage('build') {
            steps {
            	nodejs('node') {
	            powershell 'npm run install'
                powershell 'npm run test'
	            }
            }
        }
    }
    post {

	    always {
	            echo 'Publishing reports'
		        junit 'tests/reports/*.xml'      
	    }
    }
}