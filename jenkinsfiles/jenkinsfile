pipeline {
    agent any 
    stages {
        stage('build') {
            steps {
            	nodejs('node') {
	            sh """
			    npm install 
	            npm run pastebintest
			    """
	            }
            }
        }
    }
    post {

	    always {
	            echo 'Publishing reports'
		        junit 'tests/reports/*.xml'      
	    }
    }
}