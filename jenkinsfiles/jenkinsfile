pipeline {
    agent any 
    stages {
        stage('build') {
            steps {
            	nodejs('node') {
	            bat 'npm run install'
                bat 'npm run test'
	            }
            }
        }
    }
    post {

	    always {
	            echo 'Publishing reports'
		        junit 'tests/reports/*.xml'      
	    }
    }
}